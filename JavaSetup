Java version check cmd: 
java -version or javac -version

Contrl Panel --> Programs --> Java 
verify JRE path in above place

Environment varaibles need JDK path in system. Eclipse required JRE path in JRE installed options
